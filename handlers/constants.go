package handlers

// Media type string of request/response headers.
var CONTENT_TYPE = "Content-Type"

// To be set as the value of Content-Type if response body contains data encoded as JSON.
var APPLICATION_JSON = "application/json"
